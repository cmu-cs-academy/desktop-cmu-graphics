[build-system]
requires = ["maturin>=1.12,<2.0"]
build-backend = "maturin"

[project]
name = "cmu-graphics-helpers"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: Implementation :: PyPy",
]
dynamic = ["version"]

[tool.maturin]
# maturin's CLI builds debug unless told otherwise, while its PEP 517 backend
# (what uv uses) always builds release. Set the profile here so that all call
# sites use release.
profile = "release"

[tool.uv]
# uv's default cache invalidation for this path dependency doesn't notice
# edits to the Rust source, so `uv run` would keep using a stale build.
cache-keys = [
    { file = "Cargo.toml" },
    { file = "Cargo.lock" },
    { file = "pyproject.toml" },
    { file = "src/**/*.rs" },
]
